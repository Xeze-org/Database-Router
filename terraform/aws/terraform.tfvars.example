# AWS — copy to terraform.tfvars and fill in.
# Credentials come from env vars:
#   AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY  (compute)
#   CLOUDFLARE_API_TOKEN                       (DNS)
# Region is set via the `region` variable below.
# Passwords are auto-generated — shown after `terraform apply`.

# IMPORTANT: restrict to your IP! Find yours: curl ifconfig.me
allowed_ips = ["YOUR_IP/32"]

# DNS (managed in Cloudflare)
domain          = "0.xeze.org"
subdomain       = "db"
cloudflare_zone = "xeze.org"

# Compute (optional overrides)
# region        = "us-east-1"
# instance_size = "t3.small"
# image         = ""   # empty = latest official Debian 12 AMI
