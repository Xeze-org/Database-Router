# Compute provider — reads GOOGLE_CREDENTIALS (service-account JSON) and
# GOOGLE_PROJECT from the environment.
provider "google" {}

# DNS provider — reads CLOUDFLARE_API_TOKEN from the environment.
provider "cloudflare" {}
